# 디자인 가이드 (Design Guide)

이 문서는 UI/UX 디자인 원칙과 컴포넌트 스타일링 가이드를 정의합니다. 프로젝트를 추가 개발하거나 컴포넌트를 생성할 때 이 가이드를 반드시 준수해 주세요.

---

## 1. 📐 모서리 라운드 (Border Radius) 규칙

### 🚨 기본 원칙: 뾰족한 모서리 금지 & 최소 5px 라운드
- 모든 버튼, 박스, 모달, 패널, 칩, 배지, 입력 폼 등 모든 Visual Component에는 **뾰족한 직각 모서리(0px)** 사용을 금지합니다.
- 모든 요소의 라운드는 **최소 5px 이상**이어야 합니다.

### 🎨 라운드 가이드 스케일 (Tailwind CSS 기준)
| 구분 | 최소 추천 크기 | Tailwind 클래스 | 픽셀 수치 | 적용 대상 |
| :--- | :--- | :--- | :--- | :--- |
| **최소 기준** | Minimum | `rounded-md` / `rounded-[5px]` | $\ge 5\text{px}$ (6px) | 배지, 칩, 소형 태그, 툴팁 |
| **기본 컨트롤** | Medium | `rounded-xl` | $12\text{px}$ | 소형 버튼, 입력 필드, 드롭다운 |
| **메인 버튼 / 카드** | Large | `rounded-2xl` | $16\text{px}$ | 주요 Action 버튼, 정보 카드리스트 |
| **대형 패널 / 모달** | Extra Large | `rounded-3xl` | $24\text{px}$ | 모달 팝업, 메인 대시보드 컨테이너 |
| **원형 요소** | Full | `rounded-full` | $9999\text{px}$ | 프로필 아이콘, 둥근 토글/아이콘 버튼 |

---

## 2. 📢 광고 배너(toss-banner-ad) 영역 제외 계산 및 중앙 정렬 규칙

- **모달 및 팝업의 위치 계산**: 상단 또는 하단에 광고 배너(`toss-banner-ad`)가 존재하는 경우, 광고 배너가 차지하는 영역을 제외한 **순수 컨텐츠 뷰포트 영역의 정중앙**에 모달과 팝업이 위치해야 합니다.
- **전체 화면 시각 효과 및 좌표 계산**: 화면 시각 효과(파티클, 풀스크린 배경, 인터랙션 등)나 높이/좌표 계산 시에도 광고 배너 영역을 제외한 공간을 전체 화면 기준으로 계산해야 합니다.

---

## 3. 🎯 텍스트 & 아이콘 수직 중앙 정렬 (Vertical Centering) 및 미세 위치 조정

- **수직 중앙 정렬 필수**: 이모지나 아이콘 옆에 타이틀 또는 텍스트가 나란히 배치되는 경우, 텍스트가 상단으로 치우치는 현상을 방지하고 아이콘과 텍스트의 중심선이 정확히 일치하도록 수직 중앙 정렬(`align-items: center` / `items-center`)을 엄격하게 적용합니다.
- **미세 위치 보정**: 폰트 크기 변경이나 아이콘 크기 차이로 인해 상/하 균형이 맞지 않을 경우 마진과 패딩을 미세 조정하여 완벽한 시각적 균형을 유지합니다.

---

## 4. 🖥️ 화면 높이 꽉 채우기 & 불필요한 스크롤 방지 및 영역 분할

- **남은 화면 높이 100% 채우기 (`flex: 1`, `height: 100%`)**: 메인 작업 영역(에디터, 캔버스, 뷰어, 업로드 영역 등)이 화면 하단까지 빈 공간(Dead Space) 없이 꽉 채우도록 구성합니다.
- **전체 페이지 스크롤 제거**: 기본 레이아웃에서 전체 윈도우 스크롤바가 생기지 않도록 컴포넌트 높이를 뷰포트에 맞추고, 데이터나 컨텐츠가 넘칠 때만 해당 내부 컨테이너에 스크롤을 적용합니다.
- **패널 구분선(Divider) 크기 조절**: 좌우/상하로 분할된 뷰(에디터, 샌드박스 등) 사이에는 드래그 가능한 Divider를 제공하여 사용자가 영역 너비와 높이를 유연하게 조절할 수 있게 합니다.

---

## 5. 🔤 반응형 타이포그래피 & 글자 크기 조절 및 말줄임(...) 방지

- **말줄임표(`...`) 방지 및 폰트 축소**: 모바일 등 좁은 화면 뷰포트에서도 타이틀이나 필터 텍스트가 `...`으로 잘려 표시되지 않도록 화면 너비에 맞추어 폰트 크기를 유동적으로 축소합니다.
- **불필요한 줄바꿈 방지**: 버튼이나 배지 내부 문구가 한두 글자 차이로 어색하게 두 줄로 꺾이지 않도록 너비를 유연하게 확보하거나 반응형 행/열 배치를 전환합니다.
- **동적 폰트 크기 조절 컨트롤**: 문제 풀이 뷰 등 가독성이 중요한 화면에서는 사용자가 직접 글자 크기를 키우고 줄일 수 있는 `+` / `-` 버튼 컨트롤러를 제공합니다.

---

## 6. 📱 모바일 뷰포트 대응 & 탭바 안전 영역(Safe Area) 확보

- **하단 탭바 가림 방지 (안전 여백 확보)**: 스크롤을 최하단까지 내려도 본문 컨텐츠나 액션 버튼이 고정 탭바(Tab Bar)에 가려지지 않도록 모든 페이지 하단에 탭바 높이만큼의 충분한 안전 마진/패딩을 일괄 확보합니다.
- **모바일 전용 레이아웃 분기**: 데스크톱/태블릿에서는 다중 분할 고정 뷰를 유지하고, 모바일 화면에서는 세로 스크롤 및 모달 형태(예: 사이드바 → 모달 Explorer)로 최적화하여 반응형 전환을 지원합니다.
